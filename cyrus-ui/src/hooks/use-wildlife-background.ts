/**
 * Legacy hook name — returns the global dashboard crack texture URL.
 * Prefer importing `DASHBOARD_CRACK_TEXTURE_URL` from `@/lib/dashboard-backdrop`.
 */
export function useWildlifeBackground(): string {
  return "/dashboard-crack-texture.png";
}
